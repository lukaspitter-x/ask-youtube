# Python deps for ask-youtube. Install into a venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
yt-dlp          # download + transcript + comments
Pillow          # frame image handling
imagehash       # perceptual dedup of near-identical frames
markdown        # render analysis.md -> analysis.html
pytest          # tests/
